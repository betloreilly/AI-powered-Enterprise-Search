# docs/archive_CLIP_IMAGE_EMBEDDINGS.md (Re‑archived)

This file documented an older CLIP‑based image embedding approach.  
The current implementation uses a browser‑side MobileNet feature‑vector model instead.  
This note is archived under `docs/archive/archive_CLIP_IMAGE_EMBEDDINGS.md`.

